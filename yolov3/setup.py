#!/usr/bin/env python
"""Setup script for the yolov3 part of the project.

Reads metadata and binds from `setup.cfg` to build and generate entry points.
"""
from setuptools import setup


if __name__ == "__main__":
    setup()
